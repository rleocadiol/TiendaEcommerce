@{
    ViewBag.Title = "Home Page";
}

<h3 class="text-center">Seleccione alguna de las categorías</h3>
<div class="row justify-content-md-between align-items-center">
    <div class="col-md-3">
        <div class="text-center categoria">
            <a href="@Url.Action("prodCategoria", "catalogo", new { id})">content</a>
        </div>
    </div>
</div>